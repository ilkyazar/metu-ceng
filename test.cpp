#include <iostream>
#include <vector>

using namespace std;

void uniqueTest() {
    vector<int> q;
    q.push_back(2);
    q.push_back(1);
    q.push_back(3);
    q.push_back(8);
    q.push_back(7);
}